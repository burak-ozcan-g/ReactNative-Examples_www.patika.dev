import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        marginBottom :10,
    },
    text: {
        fontSize: 20,
        color:'white',
    },
    card:{
        padding: 10,
        borderRadius: 4,
    }
})